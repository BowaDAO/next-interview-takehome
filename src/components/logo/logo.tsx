import { Typography } from "@mui/material";

const Logo = () => {
  return (
    <Typography
      component="h3"
      sx={{
        fontSize: "24px",
        lineHeight: "32px",
        fontWeight: 600,
        letterSpacing: "0.1px",
        color: "#252B42",
      }}
    >
      Bandage
    </Typography>
  );
};

export default Logo;
